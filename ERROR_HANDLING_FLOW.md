# Error Handling Flow Diagram

## Overall Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        User Interface                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   React Component                         │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │           ErrorBoundary Wrapper                    │  │  │
│  │  │  ┌──────────────────────────────────────────────┐ │  │  │
│  │  │  │  - CapabilityBanner                          │ │  │  │
│  │  │  │  - PermissionError                           │ │  │  │
│  │  │  │  - QuotaExceededError                        │ │  │  │
│  │  │  │  - Content (if no errors)                    │ │  │  │
│  │  │  └──────────────────────────────────────────────┘ │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                        API Routes Layer                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  1. Check Capabilities (system.query_log accessible?)    │  │
│  │     └─► NO → Return 403 Permission Error                 │  │
│  │     └─► YES → Continue                                    │  │
│  │                                                           │  │
│  │  2. Check Cache (data already fetched?)                  │  │
│  │     └─► YES → Return cached response (fast!)             │  │
│  │     └─► NO → Continue                                     │  │
│  │                                                           │  │
│  │  3. Execute Query Safely (executeQuerySafe)              │  │
│  │     └─► Permission Error → Return 403                    │  │
│  │     └─► Quota Error → Return 429                         │  │
│  │     └─► Other Error → Return 500                         │  │
│  │     └─► Success → Cache & Return 200                     │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ClickHouse Client Layer                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  executeQuerySafe() {                                     │  │
│  │    try {                                                  │  │
│  │      result = client.query(...)                          │  │
│  │      return { success: true, data: result }              │  │
│  │    } catch (error) {                                     │  │
│  │      parsed = parseClickHouseError(error)                │  │
│  │      return {                                            │  │
│  │        success: false,                                   │  │
│  │        error: parsed,                                    │  │
│  │        permissionDenied: ...,                            │  │
│  │        quotaExceeded: ...                                │  │
│  │      }                                                    │  │
│  │    }                                                     │  │
│  │  }                                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                       ClickHouse Server                          │
│  - system.query_log (may be restricted)                         │
│  - system.clusters (may be restricted)                          │
│  - system.parts (may be restricted)                             │
│  - Quota limits (may be exceeded)                               │
└─────────────────────────────────────────────────────────────────┘
```

## Request Flow with Error Handling

### Scenario 1: Successful Request with Cache Hit

```
User clicks "Query Analyzer"
         │
         ▼
┌─────────────────────────┐
│ React Component         │
│ fetchData()             │
└─────────────────────────┘
         │
         ▼ GET /api/clickhouse/query-analyzer/aggregate
┌─────────────────────────┐
│ API Route               │
│ 1. Check capabilities ✓ │
│ 2. Check cache → HIT!  │
│ 3. Return cached (8ms) │
└─────────────────────────┘
         │
         ▼
┌─────────────────────────┐
│ React Component         │
│ setData(response)       │
│ Display results         │
└─────────────────────────┘
```

### Scenario 2: Permission Denied

```
User clicks "Query Analyzer"
         │
         ▼
┌─────────────────────────┐
│ React Component         │
│ fetchData()             │
└─────────────────────────┘
         │
         ▼ GET /api/clickhouse/query-analyzer/aggregate
┌─────────────────────────┐
│ API Route               │
│ 1. Check capabilities   │
│    hasQueryLog = false  │
│ 2. Return 403           │
└─────────────────────────┘
         │
         ▼ 403 Permission Error
┌─────────────────────────┐
│ React Component         │
│ setError({              │
│   type: PERMISSION_..., │
│   requirements: {...}   │
│ })                      │
└─────────────────────────┘
         │
         ▼
┌─────────────────────────┐
│ UI Displays:            │
│ ┌─────────────────────┐ │
│ │ 🔒 Permission Req.  │ │
│ │                     │ │
│ │ GRANT SELECT(...)   │ │
│ │ ON system.query_log │ │
│ │                     │ │
│ │ [Retry] [Dismiss]   │ │
│ └─────────────────────┘ │
└─────────────────────────┘
```

### Scenario 3: Quota Exceeded

```
User clicks "Query Analyzer"
         │
         ▼
┌─────────────────────────┐
│ React Component         │
│ fetchData()             │
└─────────────────────────┘
         │
         ▼ GET /api/clickhouse/query-analyzer/aggregate
┌─────────────────────────┐
│ API Route               │
│ 1. Check capabilities ✓ │
│ 2. Check cache → MISS   │
│ 3. Execute query        │
└─────────────────────────┘
         │
         ▼
┌─────────────────────────┐
│ executeQuerySafe()      │
│ try {                   │
│   client.query(...)     │
│ } catch (QuotaError) {  │
│   return {              │
│     quotaExceeded: true │
│   }                     │
│ }                       │
└─────────────────────────┘
         │
         ▼ 429 Quota Exceeded
┌─────────────────────────┐
│ React Component         │
│ setError({              │
│   type: QUOTA_EXCEEDED, │
│   quotaInfo: {...},     │
│   retryAfter: 60        │
│ })                      │
└─────────────────────────┘
         │
         ▼
┌─────────────────────────┐
│ UI Displays:            │
│ ┌─────────────────────┐ │
│ │ ⏱️ Quota Exceeded   │ │
│ │                     │ │
│ │ Usage: 206/200      │ │
│ │ ███████████ 103%    │ │
│ │                     │ │
│ │ [Retry in 47s]      │ │
│ └─────────────────────┘ │
│                         │
│ Countdown timer starts  │
│ Button enabled at 0s    │
└─────────────────────────┘
```

## Capability Detection Flow

```
Application Start
       │
       ▼
┌─────────────────────────┐
│ GET /api/clickhouse/    │
│     capabilities        │
└─────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────┐
│ detectSystemCapabilities()                          │
│                                                     │
│ Test 1: SELECT 1 FROM system.query_log LIMIT 1     │
│    ├─ Success → hasQueryLog = true                 │
│    └─ Error → hasQueryLog = false                  │
│                                                     │
│ Test 2: SELECT 1 FROM system.clusters LIMIT 1      │
│    ├─ Success → hasClusters = true                 │
│    └─ Error → hasClusters = false                  │
│                                                     │
│ Test 3: SELECT 1 FROM system.parts LIMIT 1         │
│    ├─ Success → hasParts = true                    │
│    └─ Error → hasParts = false                     │
│                                                     │
│ Test 4: SELECT 1 FROM system.processes LIMIT 1     │
│    ├─ Success → hasProcesses = true                │
│    └─ Error → hasProcesses = false                 │
└─────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────┐
│ Return capabilities:    │
│ {                       │
│   hasQueryLog: false,   │
│   hasClusters: true,    │
│   hasParts: true,       │
│   hasProcesses: false,  │
│   checkedAt: "..."      │
│ }                       │
└─────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────┐
│ UI Updates:                                         │
│                                                     │
│ Query Analyzer  → 🚫 UNAVAILABLE (no query_log)    │
│ Slow Queries    → 🚫 UNAVAILABLE (no query_log)    │
│ Table Explorer  → ✓ AVAILABLE                      │
│ Table Stats     → ✓ AVAILABLE                      │
│ Cluster Info    → ✓ AVAILABLE                      │
│                                                     │
│ ⚠️ Limited Access Banner Displayed                 │
└─────────────────────────────────────────────────────┘
```

## Cache Management Flow

```
Request → Check Cache → MISS → Execute Query → Cache Result
                │
                ├─ HIT → Check Expiry → Valid → Return Cached
                │                          │
                │                          └─ Expired → Delete Entry
                │                                        └─ Execute Query
                │
                └─ After 1000+ entries → Cleanup
                                           │
                                           ├─ Remove expired
                                           └─ Remove LRU (if still >500)
```

### Cache Key Generation

```
Request: /api/query-analyzer/aggregate?days=7&sort=p99&limit=100

Cache Key Generation:
┌──────────────────────────────────────────────────┐
│ globalCache.generateKey(                         │
│   'query_analyzer',                              │
│   { days: 7, sort: p99, limit: 100 }            │
│ )                                                │
│                                                  │
│ Results in:                                      │
│ "query_analyzer:days=7&limit=100&sort=p99"      │
│                                                  │
│ Cached with TTL: 60 seconds                     │
└──────────────────────────────────────────────────┘

Same request within 60s → Cache HIT
Different params → Cache MISS → New entry
After 60s → Cache expired → Execute query
```

## Error Parsing Flow

```
ClickHouse throws error
       │
       ▼
┌─────────────────────────────────────────┐
│ parseClickHouseError(error)             │
│                                         │
│ Check error message for:                │
│  - "access denied"                      │
│  - "not enough privileges"              │
│  - "quota"                              │
│  - "has been exceeded"                  │
│  - "timeout"                            │
│  - "connection"                         │
└─────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Return structured error:                │
│ {                                       │
│   type: 'PERMISSION_DENIED',            │
│   code: 497,                            │
│   message: "original error message",    │
│   details: {                            │
│     userFriendlyMessage: "...",         │
│     canRetry: false,                    │
│     requiresAction: true                │
│   }                                     │
│ }                                       │
└─────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ API Route decides response:             │
│  - 403 for PERMISSION_DENIED            │
│  - 429 for QUOTA_EXCEEDED               │
│  - 500 for other errors                 │
└─────────────────────────────────────────┘
```

## React Error Boundary Flow

```
Component Throws Error
       │
       ▼
┌─────────────────────────────────────────┐
│ ErrorBoundary catches error             │
│                                         │
│ componentDidCatch(error, errorInfo)     │
│   this.setState({                       │
│     hasError: true,                     │
│     error: error,                       │
│     errorInfo: errorInfo                │
│   })                                    │
└─────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Render fallback UI:                     │
│ ┌─────────────────────────────────────┐ │
│ │ ⚠️ Something went wrong             │ │
│ │                                     │ │
│ │ [Error Details ▼]                   │ │
│ │   error.toString()                  │ │
│ │   errorInfo.componentStack          │ │
│ │                                     │ │
│ │ [Try Again] [Reload Page]           │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
       │
       ├─ User clicks "Try Again"
       │    └─► Reset state, re-render
       │
       └─ User clicks "Reload Page"
            └─► window.location.reload()
```

## Complete Request/Response Lifecycle

```
┌──────────────────────────────────────────────────────────────┐
│ USER ACTION: Click "Query Analyzer"                          │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│ REACT COMPONENT                                              │
│ - fetchCapabilities() → Store in state                       │
│ - fetchData() → Construct URL with params                    │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│ API ROUTE: /api/clickhouse/query-analyzer/aggregate          │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ STEP 1: Capability Check                                 │ │
│ │ const caps = await getSystemCapabilities(client)         │ │
│ │ if (!caps.hasQueryLog) → Return 403 Permission Error    │ │
│ └──────────────────────────────────────────────────────────┘ │
│                           │                                  │
│                           ▼                                  │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ STEP 2: Cache Check                                      │ │
│ │ const key = generateKey('prefix', params)                │ │
│ │ const cached = cache.get(key)                            │ │
│ │ if (cached) → Return cached (< 10ms)                     │ │
│ └──────────────────────────────────────────────────────────┘ │
│                           │                                  │
│                           ▼                                  │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ STEP 3: Execute Query                                    │ │
│ │ const result = await executeQuerySafe(client, query)     │ │
│ │                                                          │ │
│ │ if (result.permissionDenied) → Return 403               │ │
│ │ if (result.quotaExceeded) → Return 429                  │ │
│ │ if (!result.success) → Return 500                       │ │
│ └──────────────────────────────────────────────────────────┘ │
│                           │                                  │
│                           ▼                                  │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ STEP 4: Cache & Return                                   │ │
│ │ cache.set(key, data, TTL)                                │ │
│ │ return NextResponse.json({ data, cached: false })        │ │
│ └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│ REACT COMPONENT HANDLES RESPONSE                             │
│                                                              │
│ if (!response.ok) {                                          │
│   setError({ type, message, requirements, ... })            │
│   → Show PermissionError or QuotaExceededError              │
│ } else {                                                     │
│   setData(response.data)                                     │
│   → Show query results                                       │
│ }                                                            │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│ UI RENDERS                                                   │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ ErrorBoundary                                            │ │
│ │   CapabilityBanner (if limited features)                 │ │
│ │                                                          │ │
│ │   if (error.type === 'PERMISSION_DENIED')                │ │
│ │     <PermissionError />                                  │ │
│ │   else if (error.type === 'QUOTA_EXCEEDED')              │ │
│ │     <QuotaExceededError />                               │ │
│ │   else                                                   │ │
│ │     <QueryResults data={data} />                         │ │
│ └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

## Decision Tree for Error Handling

```
Execute ClickHouse Query
         │
         ├─── Success?
         │     │
         │     └─ YES → Cache → Return 200
         │
         └─ NO → Parse Error
                    │
                    ├─ Permission Denied?
                    │    │
                    │    └─ YES → createPermissionErrorResponse()
                    │            → Return 403
                    │            → Show GRANT statement
                    │            → Disable retry
                    │
                    ├─ Quota Exceeded?
                    │    │
                    │    └─ YES → formatErrorResponse()
                    │            → Return 429
                    │            → Show countdown
                    │            → Enable retry after timeout
                    │
                    ├─ Connection Error?
                    │    │
                    │    └─ YES → formatErrorResponse()
                    │            → Return 500
                    │            → Enable retry
                    │
                    ├─ Timeout?
                    │    │
                    │    └─ YES → formatErrorResponse()
                    │            → Return 500
                    │            → Enable retry
                    │
                    └─ Other?
                         │
                         └─ YES → formatErrorResponse()
                                 → Return 500
                                 → Show generic error
```

## Monitoring & Debugging Flow

```
┌────────────────────────────────────────────────────────────┐
│ Browser Console                                            │
│                                                            │
│ import { getCacheStats } from '@/lib/cache';               │
│ console.log(getCacheStats());                              │
│                                                            │
│ Output:                                                    │
│ {                                                          │
│   size: 45,              // Total cached entries           │
│   validEntries: 42,      // Non-expired entries            │
│   totalHits: 234,        // Total cache hits               │
│   hitRate: 5.57          // Avg hits per entry             │
│ }                                                          │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ Network Tab (DevTools)                                     │
│                                                            │
│ GET /api/clickhouse/query-analyzer/aggregate?days=7        │
│   Status: 200 OK                                           │
│   Time: 8ms                                                │
│   Response: { data: [...], cached: true }                  │
│                                                            │
│ GET /api/clickhouse/query-analyzer/aggregate?days=30       │
│   Status: 200 OK                                           │
│   Time: 342ms                                              │
│   Response: { data: [...], cached: false }                 │
│                                                            │
│ GET /api/clickhouse/query-analyzer/aggregate?days=7        │
│   Status: 403 Forbidden                                    │
│   Response: {                                              │
│     error: "Insufficient permissions",                     │
│     type: "PERMISSION_DENIED",                             │
│     requirements: { grant: "...", documentation: "..." }   │
│   }                                                        │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ Server Logs                                                │
│                                                            │
│ [2025-11-05 12:34:56] Capability check: hasQueryLog=false │
│ [2025-11-05 12:34:56] Returning permission error          │
│                                                            │
│ [2025-11-05 12:35:23] Cache hit for query_analyzer:days=7 │
│ [2025-11-05 12:35:23] Returning cached response (8ms)     │
│                                                            │
│ [2025-11-05 12:36:45] Quota exceeded: queries=206/200     │
│ [2025-11-05 12:36:45] Returning 429 with retryAfter=60    │
└────────────────────────────────────────────────────────────┘
```
